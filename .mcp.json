{
  "mcpServers": {
    "shadcn-ui": {
      "type": "stdio",
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "@jpisnice/shadcn-ui-mcp-server",
        "--framework",
        "vue"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
